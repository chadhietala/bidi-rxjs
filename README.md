# Bidi Input With RxJs

This a prototype for bidirectional support for input fields.

## Build

```
npm install
bower install
python -m SimpleHTTPServer 1234 // Or jsut open the file in the browser
```